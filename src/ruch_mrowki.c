#include "ruch_mrowki.h"

void ruch_mrowki(int a, int b, char* kierunek, int y_mrowki,int x_mrowki, char* plansza[a][b])
{
	//printf("%i, %i, %c, %i, %i, %s", a, b , kierunek[0], y_mrowki, x_mrowki, plansza[0][0]);
	
	/*
	//kolor startowy
	int kolor_mrowki;
	if(plansza[y_mrowki][x_mrowki]==mrowka_L[0] || y_mrowki][x_mrowki]==mrowka_P[0] || y_mrowki][x_mrowki]==mrowka_G[0] || y_mrowki][x_mrowki]==mrowka_D[0])
	{
		kolor_mrowki = 0; // biala mrowka
	}
	else if(plansza[y_mrowki][x_mrowki]==mrowka_L[1] || y_mrowki][x_mrowki]==mrowka_P[1] || y_mrowki][x_mrowki]==mrowka_G[1] || y_mrowki][x_mrowki]==mrowka_D[1])
	{
		kolor_mrowki = 1; // czarna mrowka
	}
	else
	{
			//ABERRACJA, NIEDOPUSZCZALNE, WYRZUC NAJLEPIEJ ERRORA
			
	}
	printf("\n Mrówka koloru: %i", kolor_mrowki);
	
	if(kolor_mrowki = 0) // 
	{
		
	}
	else
	{
		
	}
	switch(kierunek[0])
	{
		case 'l':
			plansza[y_mrowki][x_mrowki]=mrowka_L[kolor];
			break;
		case 'p':
			plansza[y_mrowki][x_mrowki]=mrowka_P[kolor];
			break;
		case 'g':
			plansza[y_mrowki][x_mrowki]=mrowka_G[kolor];
			break;
		case 'd':
			plansza[y_mrowki][x_mrowki]=mrowka_D[kolor];
			break;
	}
	*/
}